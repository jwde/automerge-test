name: Dispatch Test
on:
  workflow_dispatch:
    inputs:
      test_input:
        description: Test input
        required: false
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dispatched with input"
